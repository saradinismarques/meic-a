<?php

    $a = source('user');
    while (true) {
        $d = s($a, 1);
        break;
        $c = s($a, 1);
    }
    while (1 > 2) {
        $c = s($a, 1);
    }
    $sink1 = z($c);
    sink2($d);

    // break denies continuing the loop | whiles are susceptible to conditions validity.

?>